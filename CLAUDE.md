# byc-web — Claude Code entrypoint

The repository workflow is maintained in [`AGENTS.md`](./AGENTS.md). Claude Code should load that file as the shared project contract so Codex and Claude use the same shape/nav commands, canonical blueprint locations, communication language, and verification commands.

@AGENTS.md
