# Workflow locations

> 2026-08-09 · **Status: in force** · Keep the Shape board and Nav grounded plans together under `docs/blueprints/`, with `plan.md` as the single maintained status index.

## The call

- Keep the Shape board and Nav grounded plans together under `docs/blueprints/`, with `plan.md` as the single maintained status index.

## How it shows up in the system

- `docs/blueprints/plan.md` records now/next/future/shipped work; `docs/blueprints/plans/` holds code-grounded plans; `docs/codebase-map/index.html` is the repo-level navigation entry point.

## What was rejected or deferred

- A separate `docs/plans/` tree and a standing HTML board are deferred because they would create two competing sources of truth.

**Evidence.** The section recorded no evidence pointer.
